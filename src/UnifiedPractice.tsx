// @ts-nocheck
import React,{useCallback,useEffect,useRef,useState}from'react'
import SessionDashboard from'./components/SessionDashboard'

const TOPICS=[
{id:'proud-project',cat:'Interviews',title:"Walk me through a project you're proud of",points:['What the project was, in one plain sentence','The hardest problem you hit, and how you approached it','One decision you made that changed the outcome',"What you'd do differently next time"]},
{id:'about-yourself',cat:'Interviews',title:'Tell me about yourself, in two minutes',points:['Where you are today and what you actually do','The turn that got you here — one story, not a timeline',"What you're best at, with one proof point","What you're looking for next"]},
{id:'explain-simply',cat:'Work',title:'Explain your current project to a non-technical person',points:['Who it helps and what annoys them today','What your system does, without naming a single tool','One example of it working, start to finish','Why it matters to the business']},
{id:'disagree',cat:'Work',title:'Disagree with a teammate — make your case',points:['State their position fairly before you argue','Your concern, backed by one concrete example','What you propose instead','What would change your mind']},
{id:'ideal-weekend',cat:'Everyday',title:'Describe your ideal weekend',points:['How it starts — the first hour','One thing you do alone, one with people','A meal that has to be in it','Why this weekend and not a fancier one']},
{id:'teach-something',cat:'Everyday',title:'Teach something you know well',points:['Why a beginner should care','The one idea everything else hangs on','A mistake every beginner makes','How to practice it this week']},
{id:'remote-work',cat:'Opinions',title:'Remote work: better or worse for careers?',points:['Your position, stated in the first sentence','The strongest argument for the other side','Why your side still wins — one example','Who this advice does not apply to']},
{id:'ai-languages',cat:'Opinions',title:'Will AI change how we learn languages?',points:['What is broken about how people learn now','One thing AI genuinely does better','One thing it cannot replace','Your prediction for five years out']},]
const FILLER_RE=/\b(um+|uh+|umm+|hmm+|erm*|you know|i mean|basically|like|so yeah)\b/gi,LIVE_REFRESH_SECONDS=3,LIVE_CONTEXT_SECONDS=12
const CSS=`
@import url('https://fonts.googleapis.com/css2?family=GFS+Didot&family=Instrument+Sans:ital,wght@0,400;0,500;0,600;1,400&display=swap');
:root{--paper:#F6F4EF;--panel:#fff;--wash:#EFECE3;--line:#E1DDD0;--line2:#C9C3B2;--ink:#1D1B16;--dim:#6E6A5C;--faint:#98937F;--aegean:#1A56A8;--aegean-soft:rgba(26,86,168,.10);--gold:#A87C24;--gold-soft:rgba(168,124,36,.12);--clay:#C2492B;--clay-soft:rgba(194,73,43,.12);--laurel:#587947;--laurel-soft:rgba(88,121,71,.12);--didot:'GFS Didot',Georgia,serif;--sans:'Instrument Sans',-apple-system,'Segoe UI',sans-serif}
html,body{margin:0;background:var(--paper)}.pu *{box-sizing:border-box}.pu{background:var(--paper);color:var(--ink);font-family:var(--sans);line-height:1.55}.pu button{font-family:var(--sans);cursor:pointer}.wrap{max-width:920px;margin:0 auto;padding:0 28px}.mast{display:flex;justify-content:space-between;align-items:center;padding:22px 0}.word{font:400 27px var(--didot)}.word small{font:500 13px var(--sans);color:var(--aegean);margin-left:11px}.livebadge{font-size:11px;color:var(--laurel);background:var(--laurel-soft);padding:6px 10px;border-radius:99px;letter-spacing:.07em}.pick{padding-bottom:30px}.greet{padding:48px 0 30px}.greet h1{font:400 clamp(34px,5vw,46px)/1.1 var(--didot);margin:0}.greet p,.brief .lede{color:var(--dim);max-width:58ch}.section-note{color:var(--faint);font-size:14px;margin-bottom:12px}.topics{border-top:1px solid var(--line)}.topic-row{width:100%;display:flex;gap:22px;align-items:baseline;text-align:left;border:0;border-bottom:1px solid var(--line);background:none;padding:19px 4px;color:var(--ink)}.topic-row:hover{background:#fff}.cat{flex:0 0 92px;color:var(--faint);font-size:13px}.topic-row .t{flex:1;font:400 20px var(--didot)}.meta{color:var(--faint);font-size:13px}.brief{padding:50px 0 70px}.crumb{font-size:13px;color:var(--faint)}.crumb button{border:0;background:none;text-decoration:underline;color:var(--dim)}.brief h1{font:400 clamp(32px,5vw,44px)/1.12 var(--didot);max-width:24ch;margin:12px 0 8px}.brief-card{margin-top:28px;display:grid;grid-template-columns:minmax(0,1fr) 190px;gap:28px;align-items:start}.points{list-style:none;padding:0;margin:0;border-top:1px solid var(--line)}.points li{display:flex;gap:15px;padding:14px 4px;border-bottom:1px solid var(--line)}.points i{font-style:normal;color:var(--aegean)}.coach-card{background:#fff;border:1px solid var(--line);border-radius:12px;padding:17px}.coach-card b{font:400 18px var(--didot)}.coach-card p{font-size:12px;color:var(--dim);line-height:1.55}.start{margin-top:25px;border:0;background:var(--aegean);color:#fff;padding:14px 25px;border-radius:8px;font-size:15px;font-weight:600}.privacy{display:block;color:var(--faint);font-size:12px;margin-top:10px;max-width:70ch}.session{padding:36px 0 34px;min-height:calc(100vh - 70px);display:flex;flex-direction:column}.sess-top{display:flex;justify-content:space-between;align-items:center;gap:18px}.sess-topic{font:italic 20px var(--didot);color:var(--dim)}.rec{display:flex;align-items:center;gap:8px;color:var(--clay);font-size:14px}.dot{width:9px;height:9px;background:var(--clay);border-radius:50%;animation:pulse 1.5s infinite}@keyframes pulse{50%{opacity:.25}}.timer{font-size:clamp(58px,9vw,86px);font-weight:600;line-height:1;margin:28px 0 12px;letter-spacing:-.03em}.timer small{font-size:15px;font-weight:400;color:var(--faint);margin-left:14px}.statusbar{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin:4px 0 12px}.pill{font-size:12px;padding:5px 9px;border-radius:99px;background:var(--wash);color:var(--dim)}.pill.good{background:var(--laurel-soft);color:var(--laurel)}.pill.warn{background:var(--gold-soft);color:var(--gold)}.pill.bad{background:var(--clay-soft);color:var(--clay)}.micmeter{width:90px;height:5px;border-radius:99px;overflow:hidden;background:var(--wash)}.micmeter i{display:block;height:100%;background:var(--laurel);transition:width .1s}.ticks{display:grid;grid-template-columns:repeat(3,1fr);gap:30px;padding:16px 0 13px;border-bottom:1px solid var(--line)}.tick b{display:block;font-size:25px}.tick span{font-size:13px;color:var(--faint)}.tick b.bad{color:var(--clay)}.livehead{display:flex;justify-content:space-between;gap:12px;margin-top:20px;max-width:68ch}.livehead b{font-size:12px;letter-spacing:.07em;text-transform:uppercase;color:var(--faint)}.livehead span{font-size:12px;color:var(--faint);text-align:right}.live{flex:1;min-height:190px;padding:10px 0 22px;font-size:17px;line-height:1.9;max-width:68ch;overflow-wrap:anywhere}.live .hint{font-style:italic;color:var(--faint)}.fill{color:var(--clay);background:var(--clay-soft);padding:0 3px;border-radius:3px}.caption-error{font-size:13px;color:var(--gold);background:var(--gold-soft);padding:9px 11px;border-radius:7px;max-width:68ch;margin-bottom:14px}.sess-foot{display:flex;justify-content:space-between;align-items:center;gap:18px;border-top:1px solid var(--line);padding-top:18px}.prompt{color:var(--faint);font-size:13px;max-width:600px}.stop{border:1px solid var(--clay);color:var(--clay);background:#fff;padding:11px 22px;border-radius:8px;font-weight:600}.analyzing{padding:120px 0}.analyzing h2{font:italic clamp(28px,5vw,34px) var(--didot);margin:0}.analyzing p{color:var(--dim);margin-top:10px;max-width:58ch}.abar{width:min(260px,75vw);height:2px;background:var(--line);margin-top:28px;overflow:hidden}.abar i{display:block;width:40%;height:100%;background:var(--aegean);animation:slide 1.3s infinite}@keyframes slide{from{transform:translateX(-100%)}to{transform:translateX(350%)}}.results{padding:40px 0 80px}.resmeta{color:var(--faint);font-size:14px}.notice{background:var(--laurel-soft);color:var(--laurel);padding:11px 13px;border-radius:8px;margin-top:14px;font-size:13px}.actions{display:flex;gap:12px;margin-top:30px}.actions button{padding:12px 20px;border-radius:8px;font-weight:600}.primary{border:0;background:var(--aegean);color:#fff}.secondary{border:1px solid var(--line2);background:#fff;color:var(--dim)}.error{margin-top:18px;padding:13px 15px;border-radius:8px;background:var(--clay-soft);color:var(--clay)}
@media(max-width:700px){.wrap{padding:0 18px}.mast{padding:16px 0}.word{font-size:25px}.greet{padding:38px 0 25px}.topic-row{display:grid;grid-template-columns:72px 1fr;gap:4px 10px;padding:16px 2px}.topic-row .t{font-size:18px;line-height:1.3}.meta{grid-column:2;font-size:12px}.brief{padding:34px 0 55px}.brief-card{grid-template-columns:1fr}.coach-card{order:-1}.session{padding:26px 0;min-height:calc(100dvh - 62px)}.sess-top{align-items:flex-start;flex-direction:column;gap:7px}.sess-topic{font-size:18px}.timer{font-size:58px;margin:20px 0 10px}.statusbar{gap:6px}.pill{font-size:11px}.ticks{gap:10px}.tick b{font-size:22px}.tick span{font-size:11px;line-height:1.25;display:block}.livehead span{font-size:11px;max-width:52%}.live{font-size:16px;line-height:1.75;min-height:160px}.sess-foot{align-items:stretch;flex-direction:column}.stop{width:100%;padding:13px}.results{padding:28px 0 55px}.actions{position:sticky;bottom:0;background:linear-gradient(180deg,rgba(246,244,239,0),#F6F4EF 28%);padding-top:24px;padding-bottom:10px;flex-direction:column}.actions button{width:100%;padding:13px}.analyzing{padding:80px 0}}
@media(max-width:390px){.wrap{padding:0 14px}.livebadge{display:none}.topic-row{grid-template-columns:66px 1fr}.timer{font-size:52px}.tick b{font-size:20px}.tick span{font-size:10px}}
`
const fmt=s=>`${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`
function recorderType(){if(typeof MediaRecorder==='undefined')return'';return['audio/webm;codecs=opus','audio/webm','audio/mp4','audio/ogg;codecs=opus'].find(t=>MediaRecorder.isTypeSupported(t))||''}
const fileName=t=>t.includes('mp4')?'session.m4a':t.includes('ogg')?'session.ogg':'session.webm'
function Marked({text}){const r=/\b(um+|uh+|umm+|hmm+|erm*|you know|i mean|basically|like|so yeah)\b/gi,p=String(text||'').split(r);return <>{p.map((x,i)=>i%2?<span className="fill" key={i}>{x}</span>:<React.Fragment key={i}>{x}</React.Fragment>)}</>}
function wavBlob(samples,sampleRate){let length=0;for(const s of samples)length+=s.length;const out=new Float32Array(length);let pos=0;for(const s of samples){out.set(s,pos);pos+=s.length}const buf=new ArrayBuffer(44+out.length*2),v=new DataView(buf),str=(o,s)=>{for(let i=0;i<s.length;i++)v.setUint8(o+i,s.charCodeAt(i))};str(0,'RIFF');v.setUint32(4,36+out.length*2,true);str(8,'WAVE');str(12,'fmt ');v.setUint32(16,16,true);v.setUint16(20,1,true);v.setUint16(22,1,true);v.setUint32(24,sampleRate,true);v.setUint32(28,sampleRate*2,true);v.setUint16(32,2,true);v.setUint16(34,16,true);str(36,'data');v.setUint32(40,out.length*2,true);for(let i=0,o=44;i<out.length;i++,o+=2){const x=Math.max(-1,Math.min(1,out[i]));v.setInt16(o,x<0?x*0x8000:x*0x7fff,true)}return new Blob([buf],{type:'audio/wav'})}
const clean=w=>w.toLowerCase().replace(/[^a-z0-9']/g,'')
function mergeRolling(existing,incoming){const a=existing.trim().split(/\s+/).filter(Boolean),b=incoming.trim().split(/\s+/).filter(Boolean);if(!a.length)return incoming.trim();if(!b.length)return existing.trim();let best=null;const from=Math.max(0,a.length-55);for(let i=from;i<a.length;i++)for(let j=0;j<Math.min(12,b.length);j++){let n=0;while(i+n<a.length&&j+n<b.length&&clean(a[i+n])===clean(b[j+n]))n++;if(n>=3&&(!best||n>best.n))best={i,j,n}}if(best){const replace=Math.max(0,best.i-best.j);return[...a.slice(0,replace),...b].join(' ')}return[...a,...b].join(' ')}

export default function UnifiedPractice(){
 const[phase,setPhase]=useState('pick'),[topic,setTopic]=useState(null),[elapsed,setElapsed]=useState(0),[text,setText]=useState(''),[result,setResult]=useState(null),[error,setError]=useState(''),[captionState,setCaptionState]=useState('starting'),[captionError,setCaptionError]=useState(''),[micLevel,setMicLevel]=useState(0),[captionUpdates,setCaptionUpdates]=useState(0)
 const finalText=useRef(''),started=useRef(0),stream=useRef(null),mainRec=useRef(null),chunks=useRef([]),finishing=useRef(false),phaseRef=useRef('pick'),audioCtx=useRef(null),meterRaf=useRef(null),processor=useRef(null),sourceNode=useRef(null),silentGain=useRef(null),rolling=useRef([]),rollingCount=useRef(0),sinceSend=useRef(0),captionQueue=useRef(Promise.resolve())
 useEffect(()=>{phaseRef.current=phase},[phase]);useEffect(()=>()=>cleanup(),[]);useEffect(()=>{if(phase!=='session')return;const id=setInterval(()=>setElapsed(Math.max(0,Math.round((Date.now()-started.current)/1000))),250);return()=>clearInterval(id)},[phase])
 const cleanup=useCallback(()=>{if(meterRaf.current)cancelAnimationFrame(meterRaf.current);try{processor.current?.disconnect();sourceNode.current?.disconnect();silentGain.current?.disconnect();audioCtx.current?.close?.()}catch{}processor.current=sourceNode.current=silentGain.current=audioCtx.current=null;stream.current?.getTracks().forEach(t=>t.stop());stream.current=null},[])
 const sendCaptionChunk=useCallback(async blob=>{if(!blob?.size||phaseRef.current!=='session')return;setCaptionState('Peitho is catching up…');try{const f=new FormData();f.append('audio',blob,'live.wav');f.append('fileName','live.wav');const r=await fetch('/api/live-transcribe',{method:'POST',body:f}),p=await r.json().catch(()=>({}));if(!r.ok)throw new Error(p.error||'preview unavailable');if(phaseRef.current!=='session')return;if(p.transcript?.trim()){finalText.current=mergeRolling(finalText.current,p.transcript.trim())+' ';setText(finalText.current);setCaptionUpdates(n=>n+1)}setCaptionState('live captions · ~3s');setCaptionError('')}catch{setCaptionState('live preview paused');setCaptionError('The live preview is taking a break. Peitho is still recording your session, so you can keep speaking.') }},[])
 const queueWindow=useCallback((force=false)=>{const ctx=audioCtx.current;if(!ctx||!rolling.current.length)return;const refresh=Math.round(ctx.sampleRate*LIVE_REFRESH_SECONDS);if(!force&&sinceSend.current<refresh)return;sinceSend.current=0;const snapshot=rolling.current.slice(),blob=wavBlob(snapshot,ctx.sampleRate);captionQueue.current=captionQueue.current.then(()=>sendCaptionChunk(blob)).catch(()=>{})},[sendCaptionChunk])
 const setupLiveAudio=useCallback(s=>{const C=window.AudioContext||window.webkitAudioContext;if(!C)throw new Error('Live voice preview is not supported in this browser.');const ctx=new C();audioCtx.current=ctx;const source=ctx.createMediaStreamSource(s);sourceNode.current=source;const analyser=ctx.createAnalyser();analyser.fftSize=256;source.connect(analyser);const data=new Uint8Array(analyser.frequencyBinCount);const draw=()=>{if(phaseRef.current!=='session')return;analyser.getByteFrequencyData(data);setMicLevel(Math.min(100,Math.round(data.reduce((a,b)=>a+b,0)/data.length*1.7)));meterRaf.current=requestAnimationFrame(draw)};draw();const proc=ctx.createScriptProcessor?.(4096,1,1);if(!proc)throw new Error('Live voice preview is not supported in this browser.');processor.current=proc;const gain=ctx.createGain();silentGain.current=gain;gain.gain.value=0;proc.onaudioprocess=e=>{if(phaseRef.current!=='session'||finishing.current)return;const input=e.inputBuffer.getChannelData(0),copy=new Float32Array(input.length);copy.set(input);rolling.current.push(copy);rollingCount.current+=copy.length;sinceSend.current+=copy.length;const max=Math.round(ctx.sampleRate*LIVE_CONTEXT_SECONDS);while(rollingCount.current>max&&rolling.current.length>1){const first=rolling.current.shift();rollingCount.current-=first.length}if(sinceSend.current>=ctx.sampleRate*LIVE_REFRESH_SECONDS)queueWindow(false)};source.connect(proc);proc.connect(gain);gain.connect(ctx.destination);setCaptionState('live captions · ~3s')},[queueWindow])
 const analyze=async(audio,duration)=>{setPhase('analyzing');const f=new FormData(),name=fileName(audio.type);f.append('audio',audio,name);f.append('fileName',name);f.append('transcript',finalText.current.trim());f.append('topicId',topic.id);f.append('topicTitle',topic.title);f.append('durationSec',String(duration));f.append('points',JSON.stringify(topic.points));try{const r=await fetch('/api/analyze',{method:'POST',body:f}),p=await r.json();if(!r.ok)throw new Error(p.error||'review unavailable');setResult(p);setPhase('results');window.scrollTo(0,0)}catch{setError('Peitho could not finish that review just now. Please try the session once more.');setPhase('brief');window.scrollTo(0,0)}}
 const start=async()=>{setError('');setCaptionError('');setText('');setElapsed(0);setMicLevel(0);setCaptionUpdates(0);finalText.current='';chunks.current=[];rolling.current=[];rollingCount.current=0;sinceSend.current=0;captionQueue.current=Promise.resolve();finishing.current=false;if(!navigator.mediaDevices?.getUserMedia||typeof MediaRecorder==='undefined'){setError('This browser cannot start a microphone practice session.');return}try{const s=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true,autoGainControl:true}});stream.current=s;const type=recorderType(),r=type?new MediaRecorder(s,{mimeType:type}):new MediaRecorder(s);r.ondataavailable=e=>{if(e.data?.size)chunks.current.push(e.data)};r.start(1000);mainRec.current=r;started.current=Date.now();phaseRef.current='session';setPhase('session');window.scrollTo(0,0);setupLiveAudio(s)}catch{cleanup();setError('Peitho could not start the microphone. Check browser microphone permission and try again.');setPhase('brief')}}
 const finish=async()=>{if(finishing.current||phaseRef.current!=='session')return;finishing.current=true;const duration=Math.max(1,Math.round((Date.now()-started.current)/1000));queueWindow(true);const r=mainRec.current;if(!r){cleanup();setError('Peitho lost the recording session. Please try again.');setPhase('brief');return}const blob=await new Promise(resolve=>{const done=()=>resolve(new Blob(chunks.current,{type:r.mimeType||recorderType()||'audio/webm'}));if(r.state==='inactive')done();else{r.addEventListener('stop',done,{once:true});r.stop()}});phaseRef.current='analyzing';cleanup();mainRec.current=null;finishing.current=false;if(!blob.size){setError('No voice recording was captured. Please try again.');setPhase('brief');return}await analyze(blob,duration)}
 const words=text.trim()?text.trim().split(/\s+/).length:0,fillers=(text.match(FILLER_RE)||[]).length,wpm=elapsed>4?Math.round(words/(elapsed/60)):0
 return <div className="pu"><style>{CSS}</style><div className="wrap"><header className="mast"><span className="word">Peitho <small>AI speaking coach</small></span><span className="livebadge">Speak · Measure · Improve</span></header>
 {phase==='pick'&&<main className="pick"><section className="greet"><h1>Ready when you are.</h1><p>Practice a real conversation, see the habits shaping how you sound, and leave knowing what to improve next.</p></section><p className="section-note">Choose today's speaking challenge</p><div className="topics">{TOPICS.map(t=><button className="topic-row" key={t.id} onClick={()=>{setTopic(t);setPhase('brief');window.scrollTo(0,0)}}><span className="cat">{t.cat}</span><span className="t">{t.title}</span><span className="meta">3–5 min · 4 prompts</span></button>)}</div></main>}
 {phase==='brief'&&topic&&<main className="brief"><p className="crumb"><button onClick={()=>{setPhase('pick');window.scrollTo(0,0)}}>All topics</button> / {topic.cat}</p><h1>{topic.title}</h1><p className="lede">Don't script it. Use the prompts as anchors and speak the way you would to a real person.</p><div className="brief-card"><ul className="points">{topic.points.map(p=><li key={p}><i>—</i><span>{p}</span></li>)}</ul><aside className="coach-card"><b>What Peitho watches</b><p>Clarity, structure, speaking rhythm, filler pressure, language quality and how easy your response is to follow.</p></aside></div><button className="start" onClick={start}>● &nbsp; Start speaking</button><span className="privacy">Peitho keeps a lightweight live preview while you speak. Your completed session gets a deeper review when you finish.</span>{error&&<div className="error">{error}</div>}</main>}
 {phase==='session'&&<main className="session"><div className="sess-top"><span className="sess-topic">{topic.title}</span><span className="rec"><i className="dot"/> Peitho is listening</span></div><div className="timer">{fmt(elapsed)}<small>/ 05:00</small></div><div className="statusbar"><span className="pill good">voice recording</span><span className={'pill '+(captionState.includes('paused')?'bad':captionState.includes('catching')?'warn':'good')}>{captionState}</span><span className="pill">Mic</span><span className="micmeter"><i style={{width:micLevel+'%'}}/></span></div><div className="ticks"><div className="tick"><b className={fillers>8?'bad':''}>{fillers}</b><span>fillers caught live</span></div><div className="tick"><b>{wpm}</b><span>words / min</span></div><div className="tick"><b>{words}</b><span>words captured live</span></div></div><div className="livehead"><b>Live speaking preview</b><span>{captionUpdates?`${captionUpdates} updates · recent words can self-correct`:'first preview usually appears after a few seconds'}</span></div><div className="live">{text?<Marked text={text}/>:<span className="hint">Speak naturally. Peitho is learning the rhythm of your first sentence…</span>}</div>{captionError&&<div className="caption-error">{captionError}</div>}<div className="sess-foot"><span className="prompt">{topic.points[0]} · {topic.points[1]} · …</span><button className="stop" onClick={finish}>End session</button></div></main>}
 {phase==='analyzing'&&<main className="analyzing"><h2>Peitho is listening back…</h2><p>Catching the rhythm of your voice, understanding your context, and turning the session into coaching you can actually use.</p><div className="abar"><i/></div></main>}
 {phase==='results'&&result&&<main className="results"><p className="resmeta">{topic.title} · {Math.round(result.metrics.durationSec)}s · {result.metrics.words} words</p>{result.degraded&&<div className="notice"><strong>Peitho Lite completed this review.</strong> Your core coaching and measurable speaking insights are ready.</div>}<SessionDashboard result={result}/><div className="actions"><button className="primary" onClick={()=>{setResult(null);setPhase('brief');window.scrollTo(0,0)}}>Practice again</button><button className="secondary" onClick={()=>{setResult(null);setTopic(null);setPhase('pick');window.scrollTo(0,0)}}>Choose another topic</button></div></main>}
 </div></div>
}
