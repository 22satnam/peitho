# Server-only keys. Never prefix these with VITE_ and never expose them to the browser.
GROQ_API_KEY=
GEMINI_API_KEY=

# Optional model overrides
GROQ_FINAL_STT_MODEL=whisper-large-v3
GROQ_LIVE_STT_MODEL=whisper-large-v3
GROQ_FALLBACK_MODEL=openai/gpt-oss-120b
GEMINI_MODEL=gemini-3.8-flash
